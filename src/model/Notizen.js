/**
 * VARIABLEN
 * Datentypen:
 * Nummer: let variablenNamen = 17
 * String - Wort/Satz: let variablenNamen = "das hier ist ein String"
 * Boolean - Wahrheitswert: let variablenNamen3 = true
 *                          let variablenNamen4 = false
 * Array - Liste: let let variablenNamen = [let variablenNamen1, let variablenNamen2, let variablenNamen3]
 *
 * ARRAYS
 * let arrayName = [1, 2, 3, 4, 5]
 * let position = 0
 * arrayName[position] = 20
 * -> arrayName[0] = 20
 *      -> [20, 2, 3, 4, 5]
 * Neues Element am Ende: arrayName.push("neuesElement"')
 * -> [20, 2, 3, 4, 5, "neuesElement")
 * Neues Element am Anfang: arrayName.unshift("neuesElement2")
 * -> ["neuesElement2", 20, 2, 3, 4, 5,"neuesElement"]
 * Element löschen am Ende: arrayName.pop()
 * -> ["neuesElement2", 20, 2, 3, 4, 5]
 * Elemente an beliebiger Position löschen: arrayName.splice(position, anzahlElemente)
 * arrayName.splice(3, 1)
 * -> ["neuesElement2", 20, 2, 4, 5]
 * Position eines Elements: arrayName.indexOf(20) -> Position 1
 *
 * IF-Bedingungen
 *
 * wenn (variable == wert) dann
 * ansonsten
 *
 * if (variableName1 == 17){
 *     hier der auszuführende Code
 * }else{
 *     hier steht der alternative Code
 * }
 *
 * OPERATOREN
 * Variable ist gleich Wert: ==
 * Variable ist ungleich: !=
 * Variable ist größer gleich: >=       Wert ist kleiner: <=
 * Variable ist größer als: >           Wert ist kleiner: <
 *
 * SCHLEIFEN
 *
 * while-Schleife:
 *
 * while(variable < wert){
 *     variable++
 * }
 *
 * for i-Schleife
 *  Startwert und Maximalwert entscheiden, wie häufig Schleife durchläuft
 *
 * for (let position = 0; position <= 10; position++){
 *     arrayName[0] = i
 * }
 * for (let i = 10; i <= 10; i++){
 *      arrayName[10] = i
 * }
 *
 * artikelListe = ["Reis", "Gemüse", "Milch"]
 *
 * for-of-Schleife
 *
 * for (let gegenstand of artikelListe){
 *     console.debug(gegenstand)
 * }
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 */